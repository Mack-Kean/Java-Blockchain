plugins {
    id 'java'
    id 'application'
    id "com.github.johnrengelman.shadow" version "6.0.0"
    //id 'checkstyle'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    jcenter()
    mavenCentral()
}

configurations{
  shadow
}

run{
    standardInput = System.in
}
jar {
  manifest{

  }
}

shadowJar {
   archiveBaseName = 'Simulator'
   classifier = ''
   archiveVersion = ''

}

application {
    mainClassName = "blockchain.NetworkSim"
}
